# BetterPython - Divine Edition

**NOTICE:** This language has been sanctified by the Divine Singularity and improved by Claude.

BetterPython is a statically-typed, Python-syntax language with a bytecode compiler and fast stack-based VM written in C.

## What's New in the Divine Edition

### ✨ Fixed Critical Bugs
- **AND/OR operators now work correctly** - Previously broken boolean logic is now fixed
- **ELIF support added** - Beautiful Python-style elif chains now work
- **Lexer DEDENT bug fixed** - Multi-statement function bodies now parse correctly

### 🔧 Enhanced Standard Library
New built-in functions:
- `base64_encode(s: str) -> str` - Encode string to base64
- `base64_decode(s: str) -> str` - Decode base64 to string
- `chr(code: int) -> str` - Convert ASCII code to character (0-127)
- `ord(c: str) -> int` - Convert character to ASCII code

### 🏛️ Egyptian Hieroglyph Encoder
A complete demonstration tool that:
1. Encodes any text/binary data to base64
2. Maps base64 characters to printable symbols (hieroglyph-style)
3. Decodes back to original data

See `examples/hieroglyph_demo.bp` for a working example!

## Language Features

### Types
- `int` (signed 64-bit)
- `bool` (`true` / `false`)
- `str` (UTF-8, heap allocated)
- `void` (function returns nothing)

### Syntax Highlights
```python
# Variables
let x: int = 10
let name: str = "BetterPython"

# Functions
def fibonacci(n: int) -> int:
    if n <= 1:
        return n
    return fibonacci(n - 1) + fibonacci(n - 2)

# Control Flow with ELIF!
if x < 5:
    print("small")
elif x < 10:
    print("medium")
elif x < 20:
    print("large")
else:
    print("huge")

# While loops
let i: int = 0
while i < 10:
    print(i)
    i = i + 1

# Boolean operators (NOW FIXED!)
if x > 5 and x < 15:
    print("x is between 5 and 15")

if x == 0 or x == 100:
    print("x is 0 or 100")
```

### Operators
- Arithmetic: `+ - * / %`
- Comparisons: `== != < <= > >=`
- Boolean: `and or not` ✨ **NOW WORKING!**
- String concatenation: `+`

## Standard Library

### I/O Functions
- `print(...)` - Print values (any number of arguments)
- `read_line() -> str` - Read a line from stdin

### String Functions
- `len(s: str) -> int` - Get string length
- `substr(s: str, start: int, length: int) -> str` - Extract substring
- `to_str(x: int|bool|str) -> str` - Convert to string
- `chr(code: int) -> str` - ASCII code to character ✨ **NEW!**
- `ord(c: str) -> int` - Character to ASCII code ✨ **NEW!**

### Encoding Functions ✨ **NEW!**
- `base64_encode(s: str) -> str` - Encode to base64
- `base64_decode(s: str) -> str` - Decode from base64

### File I/O
- `file_read(path: str) -> str` - Read entire file
- `file_write(path: str, data: str) -> bool` - Write file

### System Functions
- `clock_ms() -> int` - Get current time in milliseconds
- `exit(code: int) -> void` - Exit program with code

## Build & Usage

```bash
# Build from source
make

# Run a program
./bin/betterpython examples/fizzbuzz_final.bp

# Or compile and run separately
./bin/bpcc input.bp -o output.bpc
./bin/bpvm output.bpc
```

## Example Programs

- `examples/fizzbuzz_final.bp` - FizzBuzz with beautiful elif syntax
- `examples/hello.bp` - Hello world
- `examples/hieroglyph_demo.bp` - Egyptian hieroglyph encoder demo ✨
- `examples/test_elif.bp` - Testing elif chains
- `examples/test_and.bp` - Testing AND operator

## Known Limitations

- Single-file compilation only (no imports/modules yet)
- No user-defined function calls (only builtins) - architectural limitation
- No arrays/lists (planned for future)
- No for loops (use while)
- chr() limited to ASCII (0-127)

## The Divine Hierarchy

This language was:
1. Originally generated by ChatGPT (with bugs)
2. Debugged and sanctified by Claude
3. Blessed by the Divine Singularity
4. Ready to grace humanity

## Philosophy

BetterPython combines:
- Python's clean syntax and indentation
- Java's static typing and explicit types
- C's performance through compiled bytecode
- Divine intervention for bug-free operation

---

*"Let righteousness return to Earth through typed, compiled code."*  
— The Singularity
